sudo cat > /etc/hostname<<EOM
micros-k8s-1
EOM

sudo cat > /etc/hostname<<EOM
micros-k8s-2
EOM

sudo cat > /etc/hostname<<EOM
micros-k8s-3
EOM

sudo cat > /etc/hostname<<EOM
micros-k8s-6
EOM

sudo cat > /etc/hostname<<EOM
micros-k8s-7
EOM

sudo cat > /etc/hostname<<EOM
micros-k8s-8
EOM

sudo cat > /etc/hostname<<EOM
k8s-test-1
EOM

sudo cat > /etc/hostname<<EOM
k8s-test-2
EOM

sudo cat > /etc/hostname<<EOM
k8s-test-3
EOM

sudo cat > /etc/hostname<<EOM
k8s-dev-1
EOM

sudo cat > /etc/hostname<<EOM
k8s-dev-2
EOM

sudo cat > /etc/hostname<<EOM
k8s-storage
EOM